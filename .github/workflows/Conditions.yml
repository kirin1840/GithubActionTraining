name: Condition
on: push
jobs: 
    skip-or-run:
        runs-on: ubuntu-latest
        steps:
          - run: echo "${CONTEXT}"
            env: 
              CONTEXT: ${{ toJSON(github) }} # githubコンテキストをJSON文字列でダンプ
          - run: echo "Hello"
            if: ${{ contains(github.run_id, '1' )}} 
