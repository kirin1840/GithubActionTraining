# ステータスチェック関数のサンプル
# ステータスチェック関数の使いどころは要確認　
# 失敗時に、エラーを通知するなどの処理が可能
name: Status check functions
on: push
jobs: 
    fail:
        runs-on: ubuntu-latest
        steps:
            - run: exit 1
            - run: echo "Hello"
              if: ${{ failure() }}
